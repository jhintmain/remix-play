/*
class Car {
    name: string  = "car";
    color: string;

    constructor(color) {
        this.color = color;
    }
    start(){
        console.log(`The ${this.color} car is starting.`);
    }
}

const bmw = new Car("red");
bmw.start(); // The red car is starti


class Bmw extends Car {
    wheels: number;

    constructor(color: string, wheels: number = 4) {
        super(color);
        this.wheels = wheels;
    }

    start() {
        console.log(`The ${this.color} BMW is starting with ${this.wheels} wheels.`);
    }
}*/
